apply plugin: 'maven'
apply plugin: 'java'

sourceSets {
    main {
        resources {
            srcDirs = ["."]
            include "**"
            exclude ".gitignore", ".github", "build", ".gradle", "README.md", "local.properties", "*.gradle", "LICENSE"
        }
   }
}
